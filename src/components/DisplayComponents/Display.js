import React from "react";

const Display = ({displayCharacters}) => <div className="display">{displayCharacters}</div>;

/*const Display = ({ displayCharacters }) => {
  return (
    <div className="new_display">
      <p className="small">{displayCharacters}</p>
      <p className="main">{displayCharacters}</p>
    </div>
  );
};*/

export default Display;
